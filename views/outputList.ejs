<!DOCTYPE html>
<html>
  <head>
    <title>User List</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>User List</h1>
    <ul>
      <%
        var list = '';
        for (i = 0; i < configuration.length; i++) {
          list += '<li><a href="mailto:' + configuration[i].secretKey + '">' + configuration[i].publicKey + '</a></li>';
        }
      %>
      <%- list %>
    </ul>
  </body>
</html>